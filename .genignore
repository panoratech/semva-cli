# Files Speakeasy must not overwrite on regeneration.

# Holds `project_name: semva`. Speakeasy's generated config omits it, so
# GoReleaser falls back to the repo name and builds semva-cli_Darwin_arm64.tar.gz
# while scripts/install.sh looks for semva_Darwin_arm64.tar.gz.
.goreleaser.yaml
