# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

# The archive name_template below interpolates .ProjectName, which GoReleaser
# otherwise defaults to the repository name — publishing twinbay-cli_Darwin_arm64.tar.gz
# while scripts/install.sh fetches twinbay_Darwin_arm64.tar.gz. See .genignore.
project_name: twinbay

before:
  hooks:
    - go mod tidy

builds:
  - id: twinbay
    main: ./cmd/twinbay
    binary: twinbay
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.buildTime={{.Date}}

archives:
  - id: twinbay
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE*

release:
  github:
    owner: panoratech
    name: twinbay-cli
  draft: false
  prerelease: auto
  mode: append

checksum:
  name_template: "checksums.txt"
